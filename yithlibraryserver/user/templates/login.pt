<!DOCTYPE html>
<html lang="en"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="base.macros['base']">

<tal:block metal:fill-slot="header-title">Log in</tal:block>

<tal:block metal:fill-slot="content">

  <h1>Log in</h1>
  <ul class="thumbnails">
    <li class="span3">
      <div class="thumbnail">
        <a href="${request.route_path('twitter_login')}?next_url=${next_url}">
          <img src="${request.static_path('yithlibraryserver:static/img/twitter-logo.png')}" alt="" />
        </a>
        <div class="caption">
          <a class="btn btn-primary" href="${request.route_path('twitter_login')}?next_url=${next_url}">Log in with Twitter</a>
        </div>
      </div>
    </li>

    <li class="span3">
      <div class="thumbnail">
        <a href="${request.route_path('facebook_login')}?next_url=${next_url}">
          <img src="${request.static_path('yithlibraryserver:static/img/facebook-logo.png')}" alt="" />
        </a>
        <div class="caption">
          <a class="btn btn-primary" href="${request.route_path('facebook_login')}?next_url=${next_url}">Log in with Facebook</a>
        </div>
      </div>
    </li>

    <li class="span3">
      <div class="thumbnail">
        <a href="${request.route_path('google_login')}?next_url=${next_url}">
          <img src="${request.static_path('yithlibraryserver:static/img/google-logo.png')}" alt="" />
        </a>
        <div class="caption">
          <a class="btn btn-primary" href="${request.route_path('google_login')}?next_url=${next_url}">Log in with Google</a>
        </div>
      </div>
    </li>

  </ul>

</tal:block>
</html>
