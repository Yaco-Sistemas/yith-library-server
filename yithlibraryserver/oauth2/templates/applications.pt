<!DOCTYPE html>
<html lang="en"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="base.macros['base']">

<tal:block metal:fill-slot="header-title">Applications</tal:block>

<tal:block metal:fill-slot="content">

  <div tal:condition="authorized_apps" tal:omit-tag="">
  <h1>Authorized Applications</h1>
  <ul>
    <li tal:repeat="app authorized_apps">${app.name} <a href="">Revoke</a></li>
  </ul>
  </div>

  <h1>Developer Applications</h1>
  <ul>
    <li tal:repeat="app applications"><a href="${request.route_url('oauth2_application_view', app=app._id)}">${app.name} (${app.main_url})</a></li>
  </ul>

  <p><a href="${request.route_url('oauth2_application_new')}">Register new application</a></p>

</tal:block>

</html>
