<!DOCTYPE html>
<html lang="en"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="base.macros['base']">

<tal:block metal:fill-slot="header-title">Revoke Authorization</tal:block>

<tal:block metal:fill-slot="content">

  <div class="hero-unit">
  <h2>Revoke authorization to application ${app.name}</h2>
  <p>Are you sure you want to revoke the authorization to the application <a href="${app.main_url}">${app.name}</a> with id ${app._id}?</p>

  <form method="post" action="${request.route_path('oauth2_revoke_application', app=app._id)}">
    <input class="btn btn-danger btn-large" type="submit" name="submit" value="Yes, I am sure" />
    <a class="btn btn-large" href="${request.route_path('oauth2_applications')}">No, take me back to the application list</a>
  </form>

  </div>

</tal:block>

</html>
